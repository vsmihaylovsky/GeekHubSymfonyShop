{% extends 'AppBundle:shop:default.html.twig' %}

{% block body_content %}
    <section>
        <div class="container">
            <div class="row">
                <div class="col-sm-3">
                    {{ include( 'AppBundle:shop:default/sidebar.html.twig' ) }}
                </div>
                <div class="col-sm-9 padding-right">
                    {% block content %}{% endblock %}
                </div>
            </div>
        </div>
    </section>
{% endblock %}